# RY5088 HE65 V2 (internal model 3417)

ID **3417** (`HE65 V2`, catalog name
`ry5088_MLCY_EPOMAKERHE65Pro`) has an enabled partial USB backend. Both
installers identify VID/PID `0x3151:0x5030`, four profiles, Windows and Mac Fn
banks, magnetic switches, main light layout `S`, and replaceable switches.
Hardware remains unverified.

The macOS loader is `6f31d22e.js` → `94ab70d6.js` → `623d2d52.js`; Windows
uses `d915b681.js` → `bdf69dc1.js` → `17dc9c62.js`. The model modules contain
matching 512-byte `defaultMatrix`, `defaultFnMatrix`, and
`defaultFnMacMatrix` arrays:

- normal: `21708b469ba9fa931202b47ac4bd70e2fee26b16ab855df20f7e7dfd52e30c9b`
- Fn Windows: `9c5d518a93a669bb815134275386180dde9d9d46da2f8878784e7b715ae49fad`
- Fn Mac: `32ca4cb7bcea56cb730f476ece616b91b5fd89da69f646112e6793adafc5d834`

The enabled backend covers identity/status, profile and key/Fn maps, macros, OS
options, main lighting, pictures, switch selection, magnetic reads and
actuation/mode/snap workflows, timed USB calibration, and schema 7 backup and
restore. The three catalog-advertised knob inputs map to normal-layer slots:
volume up **90**, volume down **91**, and mute **92**. Knob bindings do not
provide Fn-layer bindings, held-macro playback, magnetic actuation, or snap
operations. Side lighting is not advertised for this model.

The catalog switch list has seven entries and uses these wire codes: `云玉磁轴`
93, `磁玉` 1, `磁玉pro` 2, `磁玉gaming` 3, `天王` 4, `万磁王` 5, and `机械轴`
7. Generic firmware magnetic bounds apply because the catalog declares no
model-specific travel override. Sleep timers for 24G and Bluetooth accept
0–64800 seconds. Deep timer words are preserved in raw responses but are not
public controls for this model. USB is the supported transport.

Recovery preserves raw magnetic fields, inactive data, macros, pictures,
lighting, Fn layers, and model-specific settings subject to schema 7 identity
and exact firmware-version checks. Firmware management, reconnect monitoring,
GUI integration, and physical hardware verification remain outside this
partial backend.

Evidence: catalog/model blocks at `evidence/catalog-{mac,win}/devices-epomaker.json`
and `mac-main.pretty.js:54412–54449` / `win-main.pretty.js:54412–54449`; loader
mappings in `evidence/catalog-{mac,win}/loaders.json`; child modules
`evidence/models/94ab70d6.js` and Windows `bdf69dc1.js`.

Knob behavior is established by the UI's nonmagnetic-key list (Mac main
108281–108300), Fn selection exclusion (134165–134174), and held-macro
rejection (108438–108451). The default media actions establish the slot mapping.
The independent implementation is in `he_knobs.py`, used by ordinary key,
magnetic and snap validation. Raw schema-7 restore does not apply UI restrictions.

```text
epomaker --device /dev/hidrawN status
epomaker --device /dev/hidrawN sleep 120 120
epomaker --device /dev/hidrawN bind-media 90 volume-up --profile 0
epomaker --device /dev/hidrawN bind-macro 91 0 --mode count
epomaker --device /dev/hidrawN switch-type 机械轴 1
```
